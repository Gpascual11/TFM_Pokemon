# Heuristic V13

Built as a sibling of v12 (`BaseHeuristic1v1`). Adds recovery, choice-lock, sweeper reactions, conservative Tera, and matchup damage from **revealed** moves.

On the gen9 10k matrix, overall win rate sits **below** v12.
